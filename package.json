{
  "name": "face_detection_password_manager_desktop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . password-manager --platform=win32 --arch=x64 --out=dist --overwrite",
    "watch": "nodemon --watch . --ext js,html --exec electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^34.2.0",
    "electron-packager": "^17.1.2"
  },
  "dependencies": {
    "axios": "^1.7.9"
  }
}
